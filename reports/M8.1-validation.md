# M8.1 Validation Report

Date: 2026-08-25

Status: **ACCEPTED**

User feedback that the page appeared blank, followed by Runtime action timeouts, a
black Runtime after Save, an MCP App overlapping the Chat composer, and stale Runtime
identity failures returned M8.1 to `IMPLEMENTING`. Recovery diagnosis found an
acceptance-entry difference, Runtime iframe lifecycle races, hidden validation Runtime
frame throttling, missing composer-aware clipping, and lost View/Session ownership
during MCP reconnect. The minimum repairs, focused checks, fresh-profile browser
coverage, retained evidence, and both repository gate sets now pass, so M8.1 has
returned to `ACCEPTED` after user verification on 2026-08-25. The four completed debug
sessions, instrumentation blocks, loopback CSP entries, logs, env files, and Debug
Servers were removed before clean revalidation. No push or M9 work was performed.

## Baseline

- `threejs-editor-mcp`: `a5f727058f28edec2d489fc201dde4a54a783ee2`
- `dsh-uni-editor`: `0f2af68cb7272f39e03b71692e804c8ec8f484e6`
- `deepseek-harness`: no M8.1 changes
- Fixed graphics corpus: `98453747cc0678f6a5d910f38d7483596a5f9a40`

The recovery audit preserved all inherited work. Protected debug files, generated
M6 fixture state, `dsh-uni-editor/docs/promotion/**`, and unrelated
`deepseek-harness` files were not modified.

## Implemented Scope

- Stable imported Project IDs with source-to-managed revision reconciliation.
- Project-bound persistent App ownership and iframe parking by Session, MCP Server,
  and `projectId`.
- Stable-parent App iframes whose fixed layout follows the current owner host without
  reparenting the live browsing context.
- Idempotent App readiness that accepts a valid initialized notification arriving
  after the Host watchdog and ignores immutable transcript object identity changes.
- Clean, dirty, conflict, Play rollover, candidate validation, and last-good behavior.
- Session-scoped draft restoration and in-place mutation result delivery.
- Validated parameter, command, debug, and sandbox-extension capability declarations.
- Three.js-owned capture, log, and action tools with exact Runtime and Owner identity.
- Per-Owner command leases, evidence-kind binding, cancellation, timeout, and stale or
  foreign rejection.
- Validation-only hidden-frame pumping, a two-second public timeout minimum, an absolute
  terminal settlement deadline, and command deduplication before execution.
- One-shot active Runtime authorization issued only by an explicit Editor UI action.
- Bounded normalized actions, cursor logs, canvas-only image evidence, and rich MCP
  content transport.
- Automatic MCP stdio reconnect with generation rotation and Prompt hash
  deduplication.
- Server-owned `threejs-runtime-validation` Prompt and binary image/no-image guidance.

## Repository Files

### `threejs-editor-mcp`

- `docs/COMPLEX-GAME-EDITOR-PLAN.md`
- `docs/specs/editor-continuity.md`
- `package.json`
- `reports/M8.1-validation.md`
- `src/m7-runtime.ts`
- `src/server.ts`
- `src/view.ts`
- `src/workspaces.ts`
- `tests/m1-server.test.mjs`
- `tests/m81-browser.mjs`
- `tests/m81-continuity.test.mjs`
- `tests/fixtures/m81/cordis.patch.yml`
- `tests/fixtures/m81/replay.override.json`
- `tests/fixtures/m81/replay.patch.yml`

### `dsh-uni-editor`

- `src/client/active-app-action.tsx`
- `src/client/app-registry.ts`
- `src/client/frame-placement.ts`
- `src/client/index.tsx`
- `src/host.ts`
- `src/types.ts`
- `tests/app-registry.test.mjs`
- `tests/frame-placement.test.mjs`
- `tests/fixtures/prompt-server.mjs`
- `tests/fixtures/reconnect-server.mjs`
- `tests/fixtures/rich-result-server.mjs`
- `tests/package.test.mjs`
- `tests/prompt-injection.test.mjs`

Temporary recovery instrumentation was removed after user acceptance and before the
final clean gates.

## Automated Verification

Passed:

```text
threejs-editor-mcp
  node --test tests/m81-continuity.test.mjs
    14/14 passed
  pnpm run release:check
    upstream sync: 28 files
    typecheck/build: passed
    tests: 43/43 passed
    packed install and executable smoke test: passed
  pnpm run test:pack
    archive, installed executable, and packed Project smoke test: passed
  node --check tests/m81-browser.mjs
  git diff --check

dsh-uni-editor
  pnpm run check
    typecheck/build: passed
    tests: 22/22 passed
  git diff --check
```

Focused tests cover stable identity, ordinary source reconciliation, recovery after
partial import metadata, local divergence conflicts, source and Workspace hardlinks,
capability rejection, Prompt identity, per-Session Runtime ownership, exact Runtime
identity, command and evidence leases, active user grants, reconnect generation,
Prompt deduplication, persistent App rebinding, and rich image attachment transport.

## Real Browser Evidence

Environment:

```text
Hardware: Apple M4 Pro, 14 cores, 48 GB
OS: macOS 15.7.1
Browser: Google Chrome 151.0.7922.172 through Playwright
Viewport: 1440x1000
Requested DPR: 1.25
Successful clean fresh-profile root:
  /private/tmp/threejs-runtime-command-deadline-final5.wFa6Gj
Successful Harness during the run:
  http://127.0.0.1:14872
Source frames:
  /private/tmp/threejs-runtime-command-deadline-final5.wFa6Gj/frames
Corpus workspace:
  /private/tmp/threejs-m8-corpus-close.Al8c0d/repo/dev/example-gallery/examples
```

The authoritative artifacts are:

```text
browser exit:
  /private/tmp/threejs-runtime-command-deadline-final5.wFa6Gj/node-exit-code.txt
  value: 0
result JSON:
  /private/tmp/threejs-runtime-command-deadline-final5.wFa6Gj/browser.log
  SHA-256: d62543aa084341a2252b3cfca3766349e0b38a162f33e822b4bd3a131d45b0c7
run parameters:
  M81_REVISION_COUNT=20
  M81_DPR=1.25
  M81_START_TIMEOUT=120000 (default)
```

The result JSON reports:

- 20 ordinary filesystem revisions and one ordinary Agent-tool revision;
- two distinct Project IDs, two App cards, and only the expected initial load plus one
  explicit page reload;
- Session reload restoration and two distinct dirty drafts restored;
- MCP restart restoration with generation rotation from
  `724ab39c-8847-4682-b8c2-d2aef36afd80` to
  `99b63b9b-5079-491a-a497-414e671f4682`;
- HTTP 400 rejection for the stale pre-reconnect generation;
- exact active and validation Runtime identity
  `example-d1a6c9dc... / 54f73a8039fa... / be99172f... / d4c729c7...`;
- successful before/after image evidence, five-action trace including the 600-frame
  wait, paginated logs, two consecutive parked Runtime commands, and unchanged active
  Runtime state and pixels;
- ANGLE Metal on Apple M4 Pro, unmasked renderer, DPR `1.25`, and drawing-buffer
  dimensions within one pixel of `clientSize * DPR`;
- no-image intent alignment with no extra Runtime evidence tool calls and the required
  stop, request-observation, wait, and no-inference wording.

The complete assertion path also covered capability manifest fields, single-card
in-place updates, no navigation during all 20 revisions, Play-time disposal and run
rollover, last-good Runtime retention after a syntax-error candidate, agent-readable
build diagnostics, all three dirty conflict choices, fullscreen draft isolation,
frame reacquisition after reconnect, PNG signature/dimension/digest verification,
action handling, log cursor uniqueness, injected warning/exception/WebGL events,
active-world camera/selection/sync isolation, and software-renderer rejection.

Recovery provenance was checked rather than inferred:

- `/private/tmp/threejs-m81-final.By0Tuu/browser-run.log` had no numeric exit artifact
  or final JSON and ended at the old virtualized-transcript no-image locator timeout.
  Its twelve frames were therefore retained as failed-run evidence, not accepted.
- `/private/tmp/threejs-m81-success.Y1cWx5/browser-run.log` ended after the browser
  process was terminated and had no exit artifact; it was discarded.
- `/private/tmp/threejs-m81-success2.uvIFJl/browser-exit-code.txt` was `1`; its log
  timed out waiting for Frost post-reconnect readiness. The added timeout diagnostics
  did not relax any assertion.
- The successful run used a new profile/root. The no-image wait now polls authoritative
  Session history while preserving the exact wording and tool-call assertions.

The first-frame repair remains one synchronous `render(performance.now())` call after
`first-frame-scheduled`; every subsequent frame is still scheduled through
`requestAnimationFrame(render)` in the render `finally` block. The associated debug
stages and pre/post evidence were retained through acceptance and removed during
cleanup. The fixed source corpus was restored and is Git-clean.

## Manual White-Screen Recovery

The recovery reused the existing `m81-draft-reload` debug session. The
required browser sequence was applied to 14822: list tabs, open the root URL, wait,
snapshot, inspect console and network, then capture a screenshot before targeted
frame evaluation.

| ID | Result | Evidence |
|---|---|---|
| A | Rejected | 14822 remained listening; root HTML, JS, CSS, and the local Editor plugin returned HTTP 200 with non-empty bodies. |
| B | Confirmed as an entry difference | A fresh normal profile opened the populated new-Session shell with no acceptance Session selected. Selecting `打开 Interactive Pool Volume` restored the target transcript. |
| C | Rejected | No page exception, warning/error console event, failed request, HTTP error, or CSP failure was observed. |
| D | Rejected | Two Project cards, outer MCP App iframes, inner documents, and canvases bound after Session selection. |
| E | Confirmed as the product defect | Frost completed candidate validation but its active Runtime remained at `loading-active-frame`; instrumentation later measured the active iframe `load` at 5129 ms against the old 5000 ms deadline. |

The first source change in this recovery was instrumentation-only. It established an
event-loop boundary race between the iframe `load` callback and the five-second timer.
The minimum behavior repair changes only `loadM7Frame()`'s document-load timeout from
5 seconds to 30 seconds. The separate Runtime `ready` deadline remains 120 seconds.

Two fresh browser contexts then followed the corrected entry procedure:

```text
manual-white-screen-post-fix-timeout-1
  screenshot: /private/tmp/m81-manual-white-screen-post-fix-timeout-1.png
manual-white-screen-post-fix-timeout-2
  screenshot: /private/tmp/m81-manual-white-screen-post-fix-timeout-2.png
```

Both reached two visible `ready`/`editing` Runtime frames by approximately 60 seconds
with empty Runtime errors and zero console/network failures. The retained post-fix
probes include validation loads of 590/512 ms, active loads of 24/95 ms, and another
successful active load at 4842 ms near the old boundary.

## Runtime Command Timeout Recovery

### Stable-parent command delivery

The supplied `/Users/bytedanceo/Downloads/session.jsonl` contains three
`simulate_player_actions` calls that expired at the Server's unchanged 20-second
command lease. Pre-fix instrumentation showed a visible App claiming and settling a
command, followed by a virtualized App creating a command that was never claimed.
Chromium control evidence confirmed that moving the live iframe between connected DOM
parents destroys its browsing context.

The Host repair keeps every persistent App iframe under one stable fixed-position root
for its whole lifetime. Visibility is mapped to the current owner host by coordinates;
virtualization only clears that owner and makes the iframe transparent. It never moves
the iframe node. The repair also:

- makes a valid late `initialized` notification idempotently clear a Host watchdog
  error instead of permanently showing `MCP App unavailable`;
- keys initialization effects by stable Session, Project, view, resource, sandbox, and
  connection-generation values rather than immutable transcript object identity;
- at that stage, kept command and transport deadlines unchanged.

Focused post-fix command evidence reports:

```text
visible waitFrames(5): 6864 ms, claimed and settled
owner unmount: browsing context marker preserved
parked waitFrames(5), first: 1454 ms, claimed and settled
parked waitFrames(5), second: 2068 ms, claimed and settled
fullscreen Play/Stop: passed
```

The authoritative parked-polling audit is:

```text
root: /private/tmp/threejs-m81-parked-audit-3.jKFNG5
browser exit: 0
browser log SHA-256:
  e697d348e57e9b6eaecc3658a7f2e68e36b41ba9eeed26ceabefa4d6b51d75f0
parked command:
  be8c1be0-cf23-44cb-b66f-37bcfe0d976b
  created: 1787515777577
  claimed: 1787515778137
  accepted: 1787515778727
```

No timeout event was emitted. The parked iframe stayed connected, retained a
non-null `contentWindow`, preserved its marker and exact Runtime identity, and
completed the command without changing the active Runtime canvas. The active
canvas SHA-256 remained
`d6011e3f995df19eefb9d7d56a9a3f3b9a9d6471119364fe6c1178a0982392dd`
before and after validation.

The confirmed root cause is that Chromium can suspend `requestAnimationFrame`
for the hidden validation Runtime while the persistent Editor is parked. The
Runtime therefore uses a timer-driven frame fallback when frame progress stalls.
The 20-second command execution ceiling remains unchanged. Separately, the fixed
Editor surface requires Host chrome and menus to suspend iframe hit testing while an
overlay is open, so the Host actions menu and Locate control remain reachable without
moving the live iframe.

The full-suite failure at the old line 564 was a test synchronization defect. Its
captured Pool metrics already reported `sync=dirty`, `draft=restored`,
`m7.active=true`, and a nonce; the iframe-local `waitForFunction` did not poll because
Chromium suspended `requestAnimationFrame` for the offscreen Project. Project lookup now
uses explicit 100 ms outer-page polling with the same 120-second deadline and unchanged
assertions.

The next full-suite failure at the old line 603 was also test-only. The reconnect frame
showed Frost in the viewport while the test attempted to click Pool's existing, enabled
Save button. Automatically locating Pool in product code would steal the user's current
Project after reconnect, so the E2E now uses the existing `MCP Apps -> Locate` action to
activate and scroll the intended owner before the real Save click.

The authoritative clean run used a new `DSH_HOME`, Agent home, project root, browser
context, and profile installation. Only the static profile manifest was copied before
`pnpm install`; no prior Session, browser storage, generated bundle, or profile state
was reused. It completed all assertions and wrote exit code `0`:

```text
root: /private/tmp/threejs-m81-recovery-clean-final.tmq4gi
Harness during run: http://127.0.0.1:14832
browser log SHA-256:
  a7df0200307e7b43358e0f38ca1b561b621aae147a5b04d9cbceb4332c8e8cb2
Host log SHA-256:
  37420afbdfbd78464785f6e935388e5e531f8046f156bca67e4990616b122b5b
```

### Long-frame validation recovery

The later acceptance transcript showed a distinct failure: the Host claimed both
commands against the same parked Runtime identity, but click plus `waitFrames: 400`
and the following click plus `waitFrames: 180` reported `Runtime Harness command timed
out`. The retained pre-fix evidence established:

- lines 1-6: the first lease was claimed normally, while validation startup consumed
  about 5.4 seconds of the 20-second execution budget;
- lines 7-11: the 400-frame action reached only 156 frames near expiry because the
  hidden Runtime advanced through a fixed 50 ms fallback plus parked scheduling;
- lines 12-18: Runtime produced a terminal failure shortly after expiry, but the Server
  had already deleted the lease and rejected that evidence as stale;
- lines 19-23: polling resumed and the second command remained independently claimable;
- lines 25-36: the 180-frame command completed in about 15.1 seconds.

The minimal repair actively pumps frames only for a validation Runtime, leaves the
active Runtime scheduler unchanged, and remembers a claimed command before execution.
Public Runtime tools now require at least a two-second timeout, matching the one-second
Host poll cadence. Successful evidence must still arrive by `expiresAt`; only terminal
failed or cancelled diagnostics can use the settlement grace, whose absolute bound is
`expiresAt + 2000 ms`.

Post-fix evidence records:

```text
isolated original sequence:
  waitFrames(400): 9.922 seconds
  subsequent waitFrames(180): 2.067 seconds

fresh-profile full E2E:
  root: /private/tmp/threejs-runtime-command-deadline-final5.wFa6Gj
  Harness: http://127.0.0.1:14872
  node exit: 0
  browser log SHA-256:
    d62543aa084341a2252b3cfca3766349e0b38a162f33e822b4bd3a131d45b0c7
  parked commands:
    6ace6e91-2274-4ca6-a509-aec9e21f0be2
    3a397c35-66d2-4a91-96ae-f297f9319d17
```

Current post-fix log lines 360-412 show the full-suite 600-frame command
`b8646cd6-436a-4fc2-83dd-0cf1282964d4` complete in 3.197 seconds and settle with
14.122 seconds remaining. The active Runtime PNG hash stayed
`bacfbf9e58fbb7248d6425d3f4b2d6856328c78130a2040a1cbdf88e00a9fa8f`,
while validation changed from
`3b8c45fdacf4575ef7bd53b292c32d6c550bca0e71f8fb85fe55026599961e01`
to `8b8e8af788fba3a84f367ae1dbb5e61b843a05ba7b4b6d0568dde4f574dc2727`.

### Save, composer, and Runtime identity recovery

The latest acceptance report exposed three related visible symptoms. Save appeared to
enter Play and blackened the Runtime, the fixed MCP App iframe covered the sticky Chat
composer, and a later Runtime command used stale identity after Save. The first two
repairs retain the editing Runtime during an editor-only revision rollover and clip the
fixed iframe against both the conversation viewport and composer.

The remaining reconnect failure was caused by `ServerState.sync()` replacing View IDs
and deleting the existing View/Session entitlement. Historical app cards recreate their
iframe without executing `open_editor` again. Runtime registration therefore succeeded,
but `ui/update-model-context` was rejected for the unowned replacement View; startup
cleanup then released the Runtime. The Host now transfers only the verified Session set
to the same MCP tool's replacement View. It still deletes old model context and rejects
old connection generations.

Evidence:

```text
pre-fix reconnect:
  log lines 206-211: register succeeds, ownedSessions is empty, publication fails
  log line 219: the same Runtime is released

post-fix reconnect:
  log lines 110-121: replacement View registers both Runtimes and publishes fresh context
  log lines 126-133: Save changes only revision and publishes the new identity
  browser Session has no ownership rejection or publication failure

fresh-profile E2E:
  root: /private/tmp/m81-reconnect-postfx-final.BjnCf8
  Harness: http://127.0.0.1:14881
  exit: 0
  M81_REVISION_COUNT: 1 (focused recovery run)
  generation: 701fefc7-... -> 1fba6f15-...
  Runtime: f4ba14ee-... / 5bc5d31b-...
  revision: 84c951... -> 8acc8e...
  runtimeModelContextRefreshed: true
  mcpRestartRestored: true
  activeEditorIsolated: true
```

The same run completed the 600-frame validation sequence, kept active Runtime pixels
unchanged, retained `playState: "editing"` and a visible Runtime frame after Save, and
recorded composer clipping with pointer interaction disabled whenever an iframe was
fully covered or parked. The E2E history reader now accepts both supported Harness event
shapes (`data.message` and direct `data`) without changing product behavior.

The final post-cleanup E2E used
`/private/tmp/m81-clean-final3.qRpSeA` with `M81_REVISION_COUNT=20` and exited `0`.
It rotated connection generation `5a2dc552-...` to `179d29f7-...`, preserved active
Runtime raw canvas digest
`eb19dec41f6d9b30cf112775e965f6e43f51c0580ab1968e41b237c6798715bb`,
and changed validation digest from
`46736622bc848a2682dd3d56c14b7e466fa35282ac3bdc643944f8e67b012e8a`
to `2b71671e2c6dce13a93335d6e74d5f268ad7c4db36faa2225de79f0656db6eac`.
The active isolation assertion reads the raw canvas bitmap rather than a DOM-composited
locator screenshot, so status text and control-state changes cannot create false
Runtime mutations.

The earlier failed roots, screenshots, logs, and invalid saved-state probes remain
preserved and are not accepted as product results. Final risk-matched gates:

```text
threejs-editor-mcp
  pnpm run release:check
    upstream sync: 28 files
    typecheck/build: passed
    tests: 43/43 passed
    packed install and executable smoke: passed
  git diff --check: passed

dsh-uni-editor
  pnpm run check
    typecheck/build: passed
    tests: 22/22 passed
  git diff --check: passed
```

## GIF Evidence

The bundled `record-browser-gif` encoder consumed all 12 chronological frames from the
single successful run:

```text
00-open
01-update-05
02-update-10
03-update-15
04-update-20
05-playing-update
06-failed-candidate
07-two-projects
08-reconnected
09-validation-before
10-validation-after
11-validation
```

The two `768x595` Runtime evidence images were proportionally scaled and padded to the
common `1800x1250` source canvas. Every normalized input was converted to `rgb24` before
encoding; the first rejected mixed-pixel-format encode is not accepted evidence.

```text
.playwright-mcp/m81-editor-continuity-recovery-clean-final-tmq4gi.gif
SHA-256: 2098488a3c1b586d4e6c81f489bc482101b299d226940bb7604c00b2039c73a4
Encoder JSON:
  .playwright-mcp/m81-editor-continuity-recovery-clean-final-tmq4gi.encoder.json
Encoder JSON SHA-256:
  27f8eaf70d0fbd9319af47230e338f19e2bfeda4ff000c7f761866f88057d7ac
Canonical copy:
  artifacts/m81-editor-continuity.gif
Source frames: 12
Encoded frames: 190
Dimensions: 1200x834
FPS: 10
Duration: 19.0 seconds
Size: 1039131 bytes
```

`ffprobe` independently reported GIF89a, 190 frames, `1200x834`, 19.0 seconds, and
1039131 bytes. Three decoded representative PNGs under
`.playwright-mcp/m81-gif-decoded-recovery-clean-final.tmq4gi/` were visually inspected.
They show initial
Pool, revision changes, Play rollover, last-good behavior after the failed candidate,
two restored Projects, reconnect, changed validation evidence, and unchanged active
Editor state. All source, normalized, decoded, encoder JSON, and GIF paths are ignored by
Git. The GIF was not published.

## Prior Independent Review

One fresh independent reviewer inspected the complete two-repository M8.1 delivery
scope plus direct protocol callers. It ran read-only, did not spawn another Agent, and
made no repository changes.

Result:

- P0: 0
- P1: 0
- P2: 1

The P2 was the instrumentation intentionally retained at review time: production paths
then contained fire-and-forget reports to `127.0.0.1:14753`, `127.0.0.1:14754`, and
`127.0.0.1:7778`, and the App CSP allowed those loopback endpoints. A local process
bound to any of those ports could receive Session, Project, and Runtime identifiers.
That finding was handled by its earlier acceptance-gated cleanup. The later `7777`
probe was also acceptance-gated and is now removed.

The reviewer found no other P0/P1/P2 issue in protocol compatibility, authorization,
path/Session/generation isolation, Runtime identity and evidence tokens, leases,
timeouts, cancellation, reload/reconnect, draft recovery, asynchronous lifecycle,
first-frame scheduling, error mapping, or regression coverage.

Review artifacts:

```text
/tmp/m81-final-review-threejs-20260823/final_comments.json
/tmp/m81-final-review-threejs-20260823/report.html
/tmp/m81-final-review-threejs-20260823/report.md
/tmp/m81-final-review-uni-20260823/final_comments.json
/tmp/m81-final-review-uni-20260823/report.html
/tmp/m81-final-review-uni-20260823/report.md
```

The reviewer lacked the out-of-repository successful-run root and therefore repeated
the old browser evidence gap as a residual note. The exit-0 JSON and GIF evidence above
close that stale note. One genuine residual test gap remains: the 512 KiB high-noise PNG
rejection path has no dedicated negative test.

### Recovery-delta review

A second fresh, independent, read-only reviewer inspected only `loadM7Frame()`, its
callers, cancellation and cleanup paths, the recovery evidence, and relevant tests.
It reported no P0/P1 issue and made no repository edit. The review result was emitted
before the CLI wrapper was denied unrelated user-state/plugin-cache writes by the
sandbox.

The reviewer recorded two P2 residuals:

- Any finite timer retains an extreme callback-ordering boundary at its deadline.
  Raising the observed 5129 ms load from a 5-second to a 30-second budget fixes the
  demonstrated defect with margin but does not make a 30-second load impossible.
- No dedicated automated boundary test pins the 30-second value or simulates a delayed
  iframe `load`; the post-fix full-suite attempts failed in Harness/profile setup before
  this path.

It also noted the accepted longer failure latency for an iframe that never loads and
the pre-existing absence of an entry `signal.aborted` check. Current call sites cannot
pass an already-aborted signal. These are residual risks, not blockers for this narrow
repair: the 120-second Runtime readiness deadline remains independent, two fresh
post-fix browser contexts crossed the repaired path, both repository gate sets passed,
and the prior complete fresh-profile suite remains exit 0.

### Final command-timeout recovery review

One fresh independent Claude reviewer (`643875ce-5ca1-43ba-97d9-d3c841abd180`) ran
with plan/read-only permissions and no subagents after the clean browser suite, GIF,
tests, and reports were complete. It reviewed the full listed source/test/document
scope in both repositories and returned:

```text
P0: 0
P1: 0
P2: 0
```

It specifically checked stable-parent iframe ownership and coordinate mapping, late
initialized recovery, stable React effect identity, command/evidence lease ordering,
reconnect generation isolation, draft restoration ordering, Runtime cancellation, and
path/origin/CSP validation. The reviewer could not read the two `/tmp` preflight files
or execute `git diff` because its sandbox denied those operations, so it reconstructed
scope from the explicit file list, repository reports, and direct full-file reads. This
limitation did not hide a reported finding.

At review time, the reviewer treated the then-retained 14753/14754/7778 instrumentation
as an acceptance-gated residual rather than an actionable finding. It also retained the
already documented finite 30-second load-boundary risk and missing 512 KiB high-noise
PNG negative test.

Review artifacts:

```text
/tmp/m81-recovery-independent-review-20260823.json
/tmp/m81-recovery-final-review-threejs-20260823/final_comments.json
/tmp/m81-recovery-final-review-threejs-20260823/report.html
/tmp/m81-recovery-final-review-threejs-20260823/report.md
/tmp/m81-recovery-final-review-uni-20260823/final_comments.json
/tmp/m81-recovery-final-review-uni-20260823/report.html
/tmp/m81-recovery-final-review-uni-20260823/report.md
```

### Long-frame deadline review

An independent narrow review of the long-frame repair initially reported `P0=0`,
`P1=1`, and `P2=2`. The P1 found that starting a fresh two-second grace timer from a
late execution-deadline callback could extend settlement beyond the intended bound.
The Server now stores one absolute `expiresAt + 2000 ms` settlement deadline, schedules
only its remaining duration, and explicitly rejects reports after it. A fresh
independent recheck reported `P0=0` and `P1=0`.

The remaining P2 observation is non-blocking and explicit:

- the source-order deduplication regression is structural rather than a synthetic
  duplicate-delivery test, while the full E2E demonstrates two real consecutive parked
  commands completing once each.

## Debug Cleanup Gate

Completed after explicit user acceptance:

1. Removed temporary `7777-7780` debug fetch blocks from both repositories.
2. Removed the loopback CSP entries and restored the production assertion.
3. Stopped Debug Server PIDs `36217`, `35374`, `35398`, and `69886`.
4. Deleted all files for the four completed debug sessions.
5. Preserved the unrelated `halo-selection-miss` debug session unchanged.
6. Re-ran the clean browser E2E, both full repository gate sets, and both
   `git diff --check` commands.
7. Re-read the exact staged scope before committing.

## Acceptance Procedure

1. Reconnect the MCP Server while an edited Project card is open, then save one more
   change. Confirm the Editor stays in editing mode, the Runtime remains visible, and
   the next Runtime command does not report a stale identity.
2. Scroll the MCP App across the Chat composer and confirm the input remains visible
   and clickable.
3. Repeat the original parked Interactive Pool operation: click plus
   `waitFrames: 400`, then run the subsequent click plus `waitFrames: 180` against the
   same Runtime identity.
4. Confirm that both commands complete without `Runtime Harness command timed out`.
5. Compare the validation before/after frames: the validation Runtime must visibly
   change while the active Runtime remains unchanged.
6. Confirm the result JSON reports
   `activeEditorIsolated: true`, `mcpRestartRestored: true`, and
   `runtimeModelContextRefreshed: true`, with two distinct
   `parkedRuntimeConsecutiveCommands`.
7. The user selected **A: fixed** on 2026-08-25 and authorized cleanup and both
   repository commits. This does not authorize push or M9.

Failure means a missing or duplicate card, lost draft, stale-generation acceptance,
unchanged validation result, active-world mutation, undecodable image/GIF, software
renderer, wrong DPR, missing no-image pause, or mismatch with the recorded JSON.

## Proposed Commits

After explicit acceptance, debug cleanup, and successful pre-commit revalidation, the
user authorized these per-repository commits:

```text
threejs-editor-mcp
  feat: add M8.1 editor continuity and runtime evidence

dsh-uni-editor
  feat: persist project-bound MCP app sessions
```

Explicit exclusions:

- `threejs-editor-mcp/.dbg/**`
- `threejs-editor-mcp/debug-halo-selection-miss.md`
- `threejs-editor-mcp/tests/fixtures/m6/workspace/.threejs-editor/**`
- `threejs-editor-mcp/tests/fixtures/m6/workspace/ideaops-m35-validation.md`
- `threejs-editor-mcp/artifacts/**`
- `threejs-editor-mcp/.playwright-mcp/**`
- `dsh-uni-editor/docs/promotion/**`
- `dsh-uni-editor/artifacts/**`
- `dsh-uni-editor/.playwright-mcp/**`
- all existing untracked `deepseek-harness` files

## Next Decision

M8.1 is `ACCEPTED`. The authorized dual-repository commits close M8.1. Push remains
unauthorized and M9 remains pending.
