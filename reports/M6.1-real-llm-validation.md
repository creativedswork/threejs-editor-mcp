# M6.1 Real LLM Validation

Date: 2026-08-18
Status: **PASS**

## Scope

This run validated the user-facing workflow with a real DeepSeek model. It did
not load `replay.patch.yml`, `session.jsonl`, or `replay.override.json`.

```text
DSH Web Workspace picker
  -> real DeepSeek model round
  -> model-selected Three.js MCP tools
  -> official Three.js Editor command
  -> revisioned local Workspace transaction
```

The API key was supplied only to the DSH process environment and is not stored
in this repository or report.

## Environment

```text
URL:
http://127.0.0.1:51833

Provider:
deepseek-official

Model:
deepseek-v4-flash

Workspace:
/tmp/threejs-editor-real-game-20260818-0001

Project ID:
workspace-c3f059d77f7f1b733e3538ddf86c520edf1ba510416f03a937a780
```

The only command-line overlay selected the browser-operable directory picker:

```text
apps/web/tests/pin-browse-picker.overlay.yml
```

The `mcp-apps` Server configuration came from the isolated Web profile's
`cordis.patch.yml`.

## User Request

```text
请用 Three.js Editor 打开当前工程，检查 Scene，然后把 Ball 的材质颜色改成
#4CC9F0，并告诉我修改后的 revision。必须自主使用 Three.js Editor MCP tools，
不能使用普通文件工具直接修改。
```

## Model Tool Sequence

The model autonomously produced this sequence:

1. `mcp__threejs__list_projects({})`
2. `glob({ pattern: "**/*" })`
3. `mcp__threejs__open_editor({})`
4. `mcp__threejs__inspect_project({ projectId })`
5. `mcp__threejs__check_project({ projectId })`
6. `mcp__threejs__inspect_editor({ projectId })`
7. `mcp__threejs__apply_editor_commands(...)`
8. `mcp__threejs__inspect_project({ projectId })`
9. `mcp__threejs__check_project({ projectId })`

`glob` was read-only discovery. No generic filesystem write tool was called.

The generated editor operation was:

```json
{
  "type": "set_material_color",
  "objectUuid": "4d89d9b8-fc45-4972-a9d1-e91e478f03c8",
  "value": "#4CC9F0"
}
```

The Server applied `SetMaterialColorCommand`.

## Result

```text
Base revision:
0315070f98c98b710f2bef0ca4b41799ee6aaed4f09cb9659d75c22e32e7cfba

Target revision:
1927e8c3f54364d07f1936f7df15fbdd4137767378c503e4f27cd7f431d47cb4

Transaction:
committed

Changed path:
src/scene.json

Ball color:
#4cc9f0

Project check:
0 errors, 0 warnings
```

The assistant response source in the durable Session log is:

```json
{
  "kind": "model",
  "provider": "deepseek-official",
  "model": "deepseek-v4-flash"
}
```

Usage recorded by DSH:

```text
inputTokens: 115
outputTokens: 507
reasoningTokens: 173
```

## Evidence

- [`../artifacts/m61-real-llm.png`](../artifacts/m61-real-llm.png)
- SHA-256:
  `3c0dab015c6f854de2d4f73f1962cd2f28787eefa5db1d3eec0a0f9e58a7bf3a`
- [`../artifacts/m61-real-llm-final.png`](../artifacts/m61-real-llm-final.png)
- SHA-256:
  `d6420e96c08b4a82590e3ea2488b9a8b9e867dbfcbf20d035eb8ca09dba32047`
- Durable Session log:
  `/tmp/threejs-editor-real-home-20260818-0001/sessions/`
- Revision journal:
  `/tmp/threejs-editor-real-game-20260818-0001/.threejs-editor/transactions/`

The browser reported no error-level console messages. It observed two
non-blocking warnings: a duplicate MCP App `ui/initialize` warning and a Node
`MaxListenersExceededWarning`.

## Conclusion

The real model discovered the current DSH Workspace, opened it without a
model-visible path parameter, selected the typed official Editor command,
committed the exact-revision transaction, and verified the result without a
generic filesystem write.
